<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Profile</title>
</head>

<body>

    <h1>Profile</h1>

    <% if (user.cover) { %>

        <img src="/upload/<%= user.cover %>" width="150">

        <% } %>

            <h2>User: <%= user.userNickname %>
            </h2>

            <p>Pet name: <%= user.petName %>
            </p>

            <a href="/register">Register again</a>

</body>

</html>